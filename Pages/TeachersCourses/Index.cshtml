@page
@model SchoolWebApp.Pages.TeachersCourses.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TeacherCourses[0].Course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TeacherCourses[0].Teacher)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TeacherCourses) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CourseID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.TeacherID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TeacherCoursesID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TeacherCoursesID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TeacherCoursesID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
